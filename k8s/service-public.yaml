apiVersion: v1
kind: Service
metadata:
  name: 3x-ui-public
  namespace: xui
spec:
  selector:
    app: 3x-ui
  type: LoadBalancer
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
